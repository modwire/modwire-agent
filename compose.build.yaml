services:
  scaffolding-api:
    build:
      context: .
    image: modwire-mcp-runtime:dev

  mcp-adapter:
    build:
      context: .
      dockerfile: Dockerfile.adapter
    image: modwire-mcp-adapter:dev

services:
  scaffolding-api:
    build:
      context: .
    image: modwire-agent-runtime:dev

  mcp-adapter:
    build:
      context: .
      dockerfile: Dockerfile.adapter
    image: modwire-agent-adapter:dev

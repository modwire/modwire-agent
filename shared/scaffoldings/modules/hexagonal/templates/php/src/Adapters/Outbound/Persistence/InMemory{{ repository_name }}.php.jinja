<?php

declare(strict_types=1);

namespace App\{{ module_class_name }}\Adapters\Outbound\Persistence;

use App\{{ module_class_name }}\Domain\Models\{{ aggregate_name }};
use App\{{ module_class_name }}\Ports\Outbound\{{ repository_name }};

final class InMemory{{ repository_name }} implements {{ repository_name }}
{
    /** @var array<string, {{ aggregate_name }}> */
    private array $items = [];

    public function save({{ aggregate_name }} $aggregate): void
    {
        $this->items[$aggregate->aggregateId] = $aggregate;
    }

    public function get(string $aggregateId): ?{{ aggregate_name }}
    {
        return $this->items[$aggregateId] ?? null;
    }
}

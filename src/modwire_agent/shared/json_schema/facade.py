from dataclasses import dataclass
from typing import Sequence


@dataclass(frozen=True)
class JsonSchemaService:
    """Whenever there is need to define some dynamic data behind the schema, this is a place"""
    pass